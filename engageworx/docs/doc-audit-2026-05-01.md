# Documentation Audit — 2026-05-01

## 1. File Inventory

| File | Last Commit | Lines | Purpose |
|------|-------------|-------|---------|
| `CLAUDE.md` | 2026-04-30 | 101 | Standing instruction set for Claude Code sessions — build principles, architecture notes, RLS patterns, completed phases, future work |
| `EngageWorx_AI_HelpDesk_Architecture.md` | 2026-03-24 | 341 | Full system architecture spec for omnichannel AI helpdesk (v1.0, March 2026, internal) |
| `docs/oauth-gmail-integration-plan.md` | 2026-04-23 | 370 | Implementation plan for OAuth Gmail integration with inbound email auto-capture (v1.0, planning status, DO NOT BUILD) |
| `infra/freeswitch-poland/README.md` | 2026-04-16 | 167 | Infrastructure guide for Polish SIP bridge via FreeSWITCH — carrier integration without Twilio DID |
| `docs/*.sql` (23 files) | Various | ~600 total | SQL migrations — schema changes, RLS policies, seed data |

**Total markdown:** 4 files, 979 lines.

---

## 2. Overlap

| Topic | Doc A | Doc B | More Current |
|-------|-------|-------|-------------|
| **Tenant hierarchy** (direct, CSP full, CSP hybrid) | Architecture §2 (detailed, 3-model diagram) | CLAUDE.md (references entity_tier, customer_type, parent_tenant_id) | **CLAUDE.md** — reflects actual implementation including csp_partner type, pipeline_stages per tenant |
| **Escalation rules** (triggers, config, flow) | Architecture §4.2 + §5.2-5.4 (spec: 8 trigger types, escalation_mode, webhook dispatch) | CLAUDE.md (references escalation_rules table, AIConfigBuilder, recipient_picker) | **CLAUDE.md** — Architecture describes planned spec, CLAUDE.md reflects what shipped |
| **AI agent config** (per-tenant) | Architecture §4.1 (ai_agent_config table with persona_prompt, max_ai_turns, kb_enabled) | CLAUDE.md (references chatbot_configs as actual table name, system_prompt, bot_name) | **CLAUDE.md** — Architecture uses `ai_agent_config`, actual table is `chatbot_configs` |
| **Database schema** (conversations, messages, contacts, tickets) | Architecture §8 (full schema with ENUMs, FKs, all columns) | CLAUDE.md (references pipeline_stages, action_items, leads.pipeline_stage_id) | **Split** — Architecture has helpdesk schema, CLAUDE.md has pipeline/action board schema. Neither has complete picture |
| **Channel strategy** (SMS, WhatsApp, email, voice, RCS) | Architecture §3 (channel capability matrix, preference logic) | None in CLAUDE.md | **Architecture** only — but RCS is noted as vaporware in practice |
| **Vendor masking** (Twilio, SendGrid, Supabase, etc.) | CLAUDE.md §Information Protection (explicit vendor→approved language mapping) | Architecture (names Twilio, Supabase freely as internal doc) | **CLAUDE.md** is the rule; Architecture is internal-only so naming is fine |
| **Email infrastructure** (send method, provider) | CLAUDE.md (references Resend migration from SendGrid, email_send_method on tenants) | Architecture §3 (email channel listed, no provider detail) | **CLAUDE.md** — Architecture doesn't mention Resend vs SendGrid |
| **Plans/pricing** | CLAUDE.md (references platform_config, is_published, is_csp_tier, CSP pricing layer gap) | Architecture §8 tenants schema (plan ENUM: starter, growth, pro, enterprise) | **CLAUDE.md** — Architecture doesn't know about CSP tiers, is_published flag, or platform_config plans JSONB |

---

## 3. Contradictions

| Topic | Doc A says | Doc B says | Resolution needed |
|-------|-----------|-----------|-------------------|
| **AI config table name** | Architecture §4.1: `ai_agent_config` table | Actual codebase + CLAUDE.md: `chatbot_configs` table | Architecture is stale — table was renamed before implementation. Update Architecture or note as superseded. |
| **Escalation trigger types** | Architecture §4.2: 8 types (complaint, refund_billing, security, explicit_request, repeat_fail, legal, data_loss, custom) | Actual code (escalation-rules-migration.sql): 4 types (keyword, sentiment, vip_match, custom) | Architecture designed more triggers than were implemented. The shipped schema uses a simpler model. |
| **Conversation status values** | Architecture §8: `ai_active, escalated, agent_active, resolved_ai, resolved_agent, closed` | Actual code (HelpDeskModule.jsx): `open, ai_active, escalated, pending, resolved, closed, spam` | Implementation diverged from spec — more statuses, different names. |
| **Plan storage** | Architecture §8: `plan ENUM('starter','growth','pro','enterprise')` on tenants | Actual: `plan TEXT` on tenants + `plans JSONB` array on platform_config with 12 plans including CSP tiers | Architecture assumes 4-plan ENUM; reality is a TEXT field with 12 possible values from a config table. |
| **Tenant type** | Architecture §8: `tenant_type ENUM('direct','csp_full','csp_hybrid')` | Actual: `customer_type TEXT` with values like 'direct', 'csp_partner', 'agent', 'internal', 'custom' | Implementation has more types than spec, different naming (customer_type vs tenant_type). |
| **RLS approach** | Architecture: no mention of RLS | CLAUDE.md: detailed RLS patterns, recursion warnings, SECURITY DEFINER functions | Architecture predates RLS implementation entirely. |

---

## 4. Orphans

| File | Last Updated | Days Since | Referenced By | Status |
|------|-------------|-----------|---------------|--------|
| `EngageWorx_AI_HelpDesk_Architecture.md` | 2026-03-24 | **38 days** | Nothing | **ORPHAN** — not referenced by CLAUDE.md, not updated since original upload. Schema section contradicts actual implementation in 5+ places. |
| `docs/oauth-gmail-integration-plan.md` | 2026-04-23 | 8 days | Nothing | Active planning doc — marked "DO NOT BUILD" but relevant. Not orphaned yet. |
| `infra/freeswitch-poland/README.md` | 2026-04-16 | 15 days | Nothing | Infra doc for deployed system. Not orphaned — actively in use for Poland carrier. |

---

## 5. Consolidation Recommendations

### A. Keep as-is (canonical for their domain)

| File | Domain | Action |
|------|--------|--------|
| **CLAUDE.md** | Build principles, architecture notes, RLS patterns, phase tracking | **Canonical.** Continue expanding as the single source of truth for how to build. |
| **infra/freeswitch-poland/README.md** | Poland voice infrastructure | **Canonical.** Self-contained infra doc, no overlap with other docs. |
| **docs/*.sql** | Schema migrations | **Canonical.** Keep as timestamped migration history. |

### B. Needs update or deprecation

| File | Recommendation | Reason |
|------|---------------|--------|
| **EngageWorx_AI_HelpDesk_Architecture.md** | **Add "SUPERSEDED" banner + move to `docs/archive/`** | 38 days stale, 5+ contradictions with actual implementation. The helpdesk is built and working — this spec served its purpose. The living architecture is now split between CLAUDE.md (principles + patterns) and the codebase itself. Keeping it at repo root suggests it's current when it's not. |
| **docs/oauth-gmail-integration-plan.md** | **Keep, add status update** | Still in planning. Add a note at top: "Blocked by Google CASA assessment. Prerequisites met: Resend migration done, inbound email handler exists. Revisit when Google verification timeline is clear." |

### C. Missing docs to create (not urgent)

| Doc | Purpose | Priority |
|-----|---------|----------|
| Root `README.md` | Repo entry point — what is this, how to run locally, link to CLAUDE.md | Low — Rob is the primary developer and doesn't need it. Create if/when onboarding others. |
| `docs/rls-policy-inventory.md` | Living inventory of all RLS policies across all tables — what exists, what's missing, what broke | Medium — would have saved days during the RLS adventures. Generate from `pg_policies` query. |
| `docs/api-routes.md` | Index of all API endpoints with auth requirements | Low — discoverable from `api/` directory. Create if API consumers (CSP integrations) need it. |

### D. Topic ownership map

| Topic | Canonical Location | Notes |
|-------|-------------------|-------|
| Build principles & rules | CLAUDE.md | Already canonical |
| Database schema (current) | Migration SQL files in docs/ | Living history, latest migrations are truth |
| Database schema (planned/spec) | ~~Architecture doc~~ → CLAUDE.md Future Work | Move any planned schema into CLAUDE.md or delete |
| RLS patterns & policies | CLAUDE.md §Architecture Notes | Expand with policy inventory when stable |
| Pipeline stages & action board | CLAUDE.md §Architecture Notes | Already canonical |
| Escalation rules | CLAUDE.md + codebase | Architecture doc is stale on this |
| Email infrastructure | CLAUDE.md (Resend migration noted) | No separate doc needed |
| Channel strategy | ~~Architecture doc~~ | Move channel capability matrix to CLAUDE.md if still relevant, or accept codebase as truth |
| Gmail integration | docs/oauth-gmail-integration-plan.md | Keep as planning doc |
| Poland voice | infra/freeswitch-poland/README.md | Self-contained |
| Plans & pricing | platform_config in DB + CLAUDE.md notes | No separate doc needed |
| Vendor masking rules | CLAUDE.md §Information Protection | Already canonical |
| Phase tracking | CLAUDE.md §Completed Phases + §Future Work | Already canonical |
