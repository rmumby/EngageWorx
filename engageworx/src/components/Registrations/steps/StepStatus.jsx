import React from 'react';

export default function StepStatus({ session, onUpdate, onNext, onBack, C }) {
  return (
    <div style={{ color: 'rgba(255,255,255,0.5)', textAlign: 'center', padding: '40px 20px' }}>
      StepStatus — placeholder
    </div>
  );
}
