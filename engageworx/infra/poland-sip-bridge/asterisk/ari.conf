; /etc/asterisk/ari.conf
; ARI user for the poland-bridge Node service.

[general]
enabled = yes
pretty = yes

[engageworx]
type = user
read_only = no
password = changeme-ari-secret
; ⚠️ Change this password and match it in /etc/poland-bridge.env ASTERISK_ARI_PASS
